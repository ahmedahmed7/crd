apiVersion: config.example.com/v1alpha1
kind: SimpleConfig
metadata:
  name: example-config
  namespace: default
spec:
  data:
    config.yaml: |
      server: "0.0.0.0"
      port: 8080
    greeting: "Bonjour"
